let baseUrl = 'http://yd.msword.top';

let home = baseUrl+'/getIndexData';
let detail = baseUrl + '/getDetails';
let classafy = baseUrl + '/getClassify';

export default {
    home,
    detail,
    classafy
}
